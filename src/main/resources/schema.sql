CREATE TABLE pet (
                         id INT AUTO_INCREMENT PRIMARY KEY,
                         first_name VARCHAR(30),
                         pet_kind VARCHAR(30),
                         gender VARCHAR(30),
                         vaccine BOOLEAN,

);
